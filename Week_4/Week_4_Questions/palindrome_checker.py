def isPalindrome(word : str) -> bool:
        if not word: # if len(word) == 0:
            return True
        elif len(word) == 1:
            return True
        elif len(word) <= 3"
            return word[0] == word [-1]
        else: